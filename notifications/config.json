{
  "base": {
    "project": "${JOB_NAME}",
    "environment": "${BROWSER} ${BROWSER_VERSION} (${BROWSER_SIZE})",
    "comment": "Запуск ${TASK} через Jenkins",
    "reportLink": "${BUILD_URL}",
    "language": "ru",
    "allureFolder": "allure-report",
    "enableChart": true
  },
  "telegram": {
    "token": "",
    "chat": "",
    "replyTo": "",
    "templatePath": "/templates/telegram.ftl"
  }
}